# Copy this to .env and adjust if needed. .env itself is gitignored, never
# committed - compose.yaml and the generator read these at runtime.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=rideshare
